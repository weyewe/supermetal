$(document).ready(function(){
	
	var $row = $("#entry-<%= @object_id %>")
	$row.spin(false); 
	
	$row.remove(); 
	
	$.msgbox("Berhasil menghapus pre production progress dengan"+ 
				" tanggal record: <%= print_date(@object_identifier) %>");
	 
	
	
	// update summary 
	<%= render :file => "pre_production_results/update_summary", :locals => {
		:template_sales_item => @parent 
	}%>
 

});