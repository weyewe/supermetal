$(document).ready(function(){
	  
	
	<% if @parent.has_unconfirmed_production_history? %>
		
		$.msgbox("Gagal membuat  Production Progress baru. Masih ada yang belum di konfirmasi.", 
					{type: "error"});
		var $modal = $("#newEntry");
		$modal.modal('hide') ;			
	<% else %>
		<% new_form = escape_javascript(  render :partial => "production_histories/new_object_form", :locals => {
	    :new_object => @new_object , 
			:url => sales_item_production_histories_url(@parent) 
	  })%>

		$("#newEntry .modal-body").append('<%= new_form %>');

		jQuery('.prevent_double_submit').preventDoubleSubmit();
	<% end %>
		
	
	// $('.datepicker').datepicker({
	// 	format : 'dd/mm/yyyy'
	// });
	
	// $('.datepicker').datepicker( {
	// 	autoclose: true 
	// });
	
	// $('.datepicker').datepicker(  );
});