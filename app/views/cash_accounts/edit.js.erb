$(document).ready(function(){
	  
		<% new_form = escape_javascript(  render :partial => "cash_accounts/new_object_form", :locals => {
	    :new_object => @object , 
			:url => update_cash_account_url(@object)  
	  })%>
 
	$("#editEntry .modal-body").append('<%= new_form %>');
	
	jQuery('.prevent_double_submit').preventDoubleSubmit();
});