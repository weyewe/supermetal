$(document).ready(function(){
	  
		<% new_form = escape_javascript(  render :partial => "sales_items/new_derivative_object_form", :locals => {
	    :new_object => @object , 
			:url => update_derivative_sales_item_url(@parent, @object, :details => params[:details])  
	  })%>
 
	$("#editEntry .modal-body").append('<%= new_form %>');
	
	jQuery('.prevent_double_submit').preventDoubleSubmit();
});